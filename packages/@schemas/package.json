{
  "name": "@shared/schemas",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "generate-types": "node --loader ts-node/esm src/generate-types.ts",
    "build": "yarn generate-types && tsc --build",
    "clean": "rimraf dist",
    "lint": "eslint src --ext .ts,.tsx",
    "test": "yarn generate-types && jest --passWithNoTests"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^22.15.30",
    "jest": "^29.0.0",
    "rimraf": "^5.0.0",
    "ts-jest": "^29.0.0",
    "ts-node": "^10.9.2",
    "typescript": "~5.8.3",
    "zod-to-ts": "^1.2.0"
  }
}
